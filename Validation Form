#include <bits/stdc++.h>
#include "testlib.h"

using namespace std;

bool check_sub1()
{
    int N = inf.readInt(1, 20, "N");  
    inf.readEoln();
    inf.readEof();
    return true;
}

int main(int argc, char* argv[] )
{
    registerValidation(argc, argv);
    int subtask = stoi (validator.group());
    if(subtask == 1) ensuref(check_sub1(), "The tast doesn't satisfy subtask 1's constraints");
}

